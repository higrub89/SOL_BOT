# ============================================================================
# THE CHASSIS — Docker Compose Environment Template
# ============================================================================
# Este archivo es usado por docker-compose.yml para inyectar variables
# al contenedor del bot.
#
# Copia este archivo como .env:
#   cp .env.example .env
#
# ⚠️  NUNCA commitees el archivo .env — contiene secretos reales.
# ============================================================================

# ── REQUERIDO: Helius API (RPC + Geyser) ──
HELIUS_API_KEY=your_helius_api_key_here

# ── REQUERIDO: Wallet ──
WALLET_ADDRESS=your_wallet_public_address_here
WALLET_PRIVATE_KEY=your_wallet_private_key_here

# ── OPCIONAL: Jupiter API (mejora rate limits) ──
JUPITER_API_KEY=

# ── Network Settings ──
MAX_LATENCY_MS=150

# ── OPCIONAL: Telegram Notifications ──
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── PriceFeed Configuration ──
GEYSER_ENDPOINT=
DEXSCREENER_INTERVAL_SEC=5

# ── Docker Image (para CI/CD) ──
# Se usa automáticamente por docker-compose si no haces build local
# BOT_IMAGE=ghcr.io/higrub89/the_chassis:latest
